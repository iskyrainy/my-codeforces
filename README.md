# My CodeForces

## Desc
Record my CF submissions.

## Submit
1. only write in `fn solve()` of `main.rs`;
2. generate submit code by `rust_bundler_cp --input . --output ./outputs/{problem name}.rs`;
3. submit `./outputs/{problem name}.rs`;

## Test
Test code by `cargo run`.

